# 从零手工完成 NebulaGraph 数据搬迁

本手册针对当前机器：在同一个 NebulaGraph 实例中，把新建的源图空间 manual_mig_a 搬迁到工具创建的目标图空间 manual_mig_b。

本文只提供操作步骤，没有替你创建这两个案例空间。已有验收空间不需要删除。主流程需要手工执行，命令按顺序逐块复制；每一步确认成功后再继续。

## 0. 先区分两个终端和三种反斜杠

建议打开两个终端：

| 终端 | 用途 |
|---|---|
| 终端 A：Linux Bash | 构建 JAR、设置变量、运行搬迁命令、查看 CSV 和日志 |
| 终端 B：nebula-console | 执行 CREATE、INSERT、FETCH、UPDATE 等 nGQL |

所有多行 nGQL 都已经加了行尾续行符 \，最后一行以分号结束。

- 必须保留每个行尾的 \，其后不要加空格、注释或其他字符。
- 行尾的 \ 用于控制台续行；字符串内部的 \n、\r、\000 是数据转义。
- 字符串中要存储一个真实反斜杠，写成 \\。
- 不要复制控制台提示符，例如 (root@nebula)> 或 ->。
- 看到 -> 是控制台等待后续行；贴入最后一行后会执行。输错可按 Ctrl+C 清除当前未完成输入。
- :sleep 20 是控制台自身的等待命令，不是 nGQL，不加续行符。
- 本文格式针对 nebula-console；不要把带控制台续行符的内容原样当成 Java Session.execute 的字符串。

已在当前交互式控制台验证续行符、NUL、无效 UTF-8 和时间构造表达式。控制台登录及 :sleep 命令也可参考 [官方 Console 说明](https://github.com/vesoft-inc/nebula-console)。

## 1. 环境及本案例的结果

| 项目 | 当前值 |
|---|---|
| 仓库 | /home/sch/nebula/nebula-java |
| 数据库目录 | /home/sch/nebula-run/nebula-3.6 |
| Graph | 127.0.0.1:9669 |
| Meta | 127.0.0.1:9559 |
| Storage | 127.0.0.1:9779 |
| 用户、密码 | root / nebula |
| 源空间 | manual_mig_a |
| 目标空间 | manual_mig_b |
| VID | FIXED_STRING(128) |
| 分区、副本 | 3 个分区、1 个副本 |

按本文写完源数据后，预期有 6 个不同 VID、8 条 Tag 记录、4 条 Edge 记录：

| 类型 | 名称 | 记录数 |
|---|---|---:|
| TAG | all_types | 6 |
| TAG | alias_tag | 1 |
| TAG | bare_tag | 1 |
| TAG | empty_tag | 0 |
| EDGE | all_edges | 3 |
| EDGE | bare_edge | 1 |
| EDGE | empty_edge | 0 |

all_types 定义全部 14 种支持的属性类型；样本涵盖 NULL、空字符串、整数上下界、负零、微秒、特殊字符、NUL、无效 UTF-8、NFC/NFD。多 Tag、自环、不同 rank、无属性记录和空表也包含在内。超长字符串及完整浮点极值样本见第 14 节的现成验收集和专项测试。

## 2. 检查数据库与构建工具

在终端 A 执行：

~~~bash
/home/sch/nebula-run/nebula-3.6/scripts/nebula.service status metad
/home/sch/nebula-run/nebula-3.6/scripts/nebula.service status storaged
/home/sch/nebula-run/nebula-3.6/scripts/nebula.service status graphd
~~~

当前三个服务都已运行，可以跳过启动。只有对应服务未运行时，才按顺序启动：

~~~bash
/home/sch/nebula-run/nebula-3.6/scripts/nebula.service start metad
/home/sch/nebula-run/nebula-3.6/scripts/nebula.service start storaged
/home/sch/nebula-run/nebula-3.6/scripts/nebula.service start graphd
~~~

当前 JAR 已存在。要亲手完成构建，在终端 A 执行：

~~~bash
cd /home/sch/nebula/nebula-java

JAVA_HOME=/usr/lib/jvm/java-8-openjdk-amd64 \
PATH=/usr/lib/jvm/java-8-openjdk-amd64/bin:$PATH \
mvn -pl migration -am package \
  -DskipTests \
  -Dmaven.javadoc.skip=true
~~~

确认出现 BUILD SUCCESS，然后执行：

~~~bash
export MIGRATION_JAR=/home/sch/nebula/nebula-java/migration/target/migration-3.8.4.jar

java -jar "$MIGRATION_JAR" --help
~~~

本次构建命令跳过测试；第 14 节给出显式测试命令。构建需使用本仓库 client 模块，保留已有扫描修复。

在终端 A 设置本次运行目录和密码：

~~~bash
export NEBULA_PASSWORD='nebula'
export MIGRATION_CASE_DIR="/home/sch/nebula/migration-manual/$(date +%Y%m%d_%H%M%S)"
export MIGRATION_BUNDLE="$MIGRATION_CASE_DIR/bundle"

mkdir -p "$MIGRATION_CASE_DIR/logs"
printf '本次工作目录：%s\n' "$MIGRATION_CASE_DIR"
~~~

后续 Linux 命令继续在同一个终端 A 执行。这些变量不会自动出现在另一个终端或重新打开的终端里。

目录布局：

~~~text
本次工作目录/
├── logs/
└── bundle/    导出命令创建，保存 CSV、manifest.json 和成功校验报告
~~~

不要提前把日志放到 bundle 中。export 要求该目录不存在或为空；重定向日志会先创建文件，从而让导出失败。

## 3. 登录控制台并确认源、目标名称尚未使用

在终端 B 的 Linux Bash 中执行：

~~~bash
/home/sch/nebula-run/nebula-console \
  -addr 127.0.0.1 \
  -port 9669 \
  -u root \
  -p nebula
~~~

进入控制台后执行：

~~~ngql
SHOW HOSTS;
SHOW SPACES;
~~~

检查 Storage 127.0.0.1:9779 为 ONLINE。当前实例已注册 Storage，不需要再次 ADD HOSTS。

确认 manual_mig_a 和 manual_mig_b 都不存在。如果已存在，先停止本案例，换两个新名称，并同步替换下面所有命令中的源、目标名称。不要直接向已有业务空间写测试数据。

目标 B 由搬迁工具自动创建，后续不要手工 CREATE SPACE manual_mig_b。

## 4. 创建源空间及 Schema

以下全部在终端 B 的 nebula-console 中执行。

创建源空间：

~~~ngql
CREATE SPACE manual_mig_a( \
  partition_num=3, \
  replica_factor=1, \
  vid_type=FIXED_STRING(128) \
);
~~~

确认成功后等待元数据传播：

~~~text
:sleep 20
~~~

选择源空间：

~~~ngql
USE manual_mig_a;
~~~

创建包含全部 14 种支持属性类型的 Tag：

~~~ngql
CREATE TAG all_types( \
  b BOOL NULL, \
  i8 INT8 NULL, \
  i16 INT16 NULL, \
  i32 INT32 NULL, \
  i64 INT64 NULL, \
  f32 FLOAT NULL, \
  f64 DOUBLE NULL, \
  s STRING NULL, \
  fs FIXED_STRING(128) NULL, \
  date_value DATE NULL, \
  time_value TIME NULL, \
  datetime_value DATETIME NULL, \
  ts TIMESTAMP NULL, \
  dur DURATION NULL \
);
~~~

创建另外三个 Tag：

~~~ngql
CREATE TAG alias_tag(label STRING NULL);
CREATE TAG bare_tag();
CREATE TAG empty_tag(s STRING NULL);
~~~

创建边类型：

~~~ngql
CREATE EDGE all_edges( \
  s STRING NULL, \
  f64 DOUBLE NULL, \
  datetime_value DATETIME NULL \
);
~~~

~~~ngql
CREATE EDGE bare_edge();
CREATE EDGE empty_edge(s STRING NULL);
~~~

再等待一次：

~~~text
:sleep 20
~~~

检查 Schema：

~~~ngql
SHOW TAGS;
SHOW EDGES;
DESCRIBE TAG all_types;
DESCRIBE EDGE all_edges;
~~~

预期有 4 个 Tag、3 个 Edge 类型。若前面的任意 CREATE 返回错误，先排查，不继续插入。

## 5. 写入点样本

仍在终端 B 中，确认当前空间是 manual_mig_a。

第一条 v1 使用整数下界、14 种属性类型和复杂字符串：

~~~ngql
INSERT VERTEX all_types( \
  b, i8, i16, i32, i64, f32, f64, s, fs, \
  date_value, time_value, datetime_value, ts, dur \
) VALUES \
"v1":( \
  true, -128, -32768, -2147483648, toInteger("-9223372036854775808"), \
  0.1, 0.1, \
  "单引号' 双引号\" 反斜杠\\ CR\r LF\n TAB\t NUL\000 emoji😀", \
  "code😀", \
  date("2024-02-29"), time("13:14:15.123456"), \
  datetime("2024-02-29T13:14:15.123456"), 1709164800, \
  duration({months:2, seconds:3, microseconds:456789}) \
);
~~~

本例的 NUL 位于普通 STRING 属性 s 中；VID 和 FIXED_STRING 属性 fs 都不包含 NUL。

第二条 v2 使用整数上界、浮点负零和空字符串：

~~~ngql
INSERT VERTEX all_types( \
  b, i8, i16, i32, i64, f32, f64, s, fs, \
  date_value, time_value, datetime_value, ts, dur \
) VALUES \
"v2":( \
  false, 127, 32767, 2147483647, 9223372036854775807, \
  -0.0, -0.0, "", "", \
  date("2024-01-01"), time("00:00:00.000001"), \
  datetime("2024-01-01T00:00:00.000001"), 0, \
  duration({seconds:0}) \
);
~~~

再写四条记录：

~~~ngql
INSERT VERTEX all_types(s) VALUES \
"v3":(NULL), \
"v4":("\377\376\300\257"), \
"v5":("\303\251"), \
"v6":("\145\314\201");
~~~

这里所有未列出的属性均允许 NULL 且未配置默认值，因此填为 NULL。

| VID | s 的内容 |
|---|---|
| v3 | NULL；整条记录的属性都为 NULL |
| v4 | 原始字节 FF FE C0 AF，包含无效 UTF-8 |
| v5 | UTF-8 的 é，NFC，2 字节 |
| v6 | e 加组合重音符，NFD，3 字节 |

使用八进制转义能避免复制时输入法或编辑器改变组合字符。

给 v1 增加第二个 Tag 和一个无属性 Tag：

~~~ngql
INSERT VERTEX alias_tag(label) VALUES \
"v1":("第二个Tag，含逗号,引号\"");

INSERT VERTEX bare_tag() VALUES \
"v1":();
~~~

到这里，共有 6 个不同 VID，但有 8 条 Tag 记录。empty_tag 保持为空。

## 6. 写入边样本

在终端 B 执行：

~~~ngql
INSERT EDGE all_edges(s, f64, datetime_value) VALUES \
"v1"->"v2"@0:("edge\nabc\000def", 0.1, datetime("2024-02-29T13:14:15.123456")), \
"v1"->"v2"@7:(NULL, NULL, NULL), \
"v2"->"v2"@-1:("", -0.0, datetime("2024-02-29T00:00:00.000001"));
~~~

再增加一条无属性边：

~~~ngql
INSERT EDGE bare_edge() VALUES \
"v1"->"v1"@0:();
~~~

现在有 4 条边：两条相同端点不同 rank 的边、一条带属性自环、一条无属性自环。empty_edge 保持为空。

## 7. 在导出前回读源数据

先检查点。FETCH 必须带 YIELD：

~~~ngql
FETCH PROP ON all_types "v1","v2","v3","v4","v5","v6" \
YIELD \
  id(vertex) AS vid, \
  all_types.i64 AS i64_value, \
  all_types.f32 AS float_value, \
  all_types.f64 AS double_value, \
  size(all_types.s) AS s_bytes, \
  all_types.time_value AS time_value, \
  all_types.datetime_value AS datetime_value;
~~~

预期 6 行，不依赖返回顺序。s_bytes 分别为：

| VID | s_bytes |
|---|---:|
| v1 | 60，前提是原样复制本文混合字符串 |
| v2 | 0 |
| v3 | NULL |
| v4 | 4 |
| v5 | 2 |
| v6 | 3 |

v1 的 i64 是 -9223372036854775808，v2 是 9223372036854775807。浮点显示格式可能由控制台格式化，最终精确判定依赖工具的位模式比较。

检查边：

~~~ngql
FETCH PROP ON all_edges \
"v1"->"v2"@0, \
"v1"->"v2"@7, \
"v2"->"v2"@-1 \
YIELD \
  src(edge) AS src, \
  dst(edge) AS dst, \
  rank(edge) AS rank_value, \
  size(all_edges.s) AS s_bytes, \
  all_edges.f64 AS weight, \
  all_edges.datetime_value AS created;
~~~

预期 3 行：rank 为 0、7、-1；对应字符串长度为 12、NULL、0。

检查附加记录：

~~~ngql
FETCH PROP ON alias_tag "v1" \
YIELD alias_tag.label AS label;

FETCH PROP ON bare_tag "v1" \
YIELD id(vertex) AS vid;

FETCH PROP ON bare_edge "v1"->"v1"@0 \
YIELD src(edge) AS src, dst(edge) AS dst, rank(edge) AS rank_value;
~~~

确认这些读取都符合预期后，停止修改源空间。从下一步导出开始，到最终 verify 结束，源数据和 Schema 必须保持不变；目标空间由搬迁工具独占。

## 8. 导出源空间

切换到终端 A。先确认变量仍有效：

~~~bash
printf 'JAR=%s\nBUNDLE=%s\n' "$MIGRATION_JAR" "$MIGRATION_BUNDLE"
~~~

导出：

~~~bash
java -Xmx2g -jar "$MIGRATION_JAR" export \
  --source-space manual_mig_a \
  --directory "$MIGRATION_BUNDLE" \
  --source-host 127.0.0.1 \
  --source-graph-port 9669 \
  --source-meta-port 9559 \
  --source-user root \
  --source-password-env NEBULA_PASSWORD \
  --scan-limit 1 \
  --timeout-ms 60000 \
  > "$MIGRATION_CASE_DIR/logs/export.log" 2>&1

MIGRATION_EXIT=$?
cat "$MIGRATION_CASE_DIR/logs/export.log"
printf 'export exit=%s\n' "$MIGRATION_EXIT"
~~~

只有 exit=0 才继续。这里 limit=1 是为了让小样本也经历多页扫描，不代表只能导出一条记录。

查看文件：

~~~bash
ls -lh "$MIGRATION_BUNDLE"
python3 -m json.tool "$MIGRATION_BUNDLE/manifest.json"
~~~

预期 7 个 CSV，以及 manifest.json。编号文件和真实 Tag/Edge 的对应关系在清单 tables 中，不根据编号猜测。

注意：Java 命令返回后立即保存退出码，不能先执行 cat 再读取 $?。日志在外层 logs，不能重定向到 bundle 里面。

## 9. 离线检查 CSV

在终端 A 执行：

~~~bash
java -Xmx2g -jar "$MIGRATION_JAR" inspect \
  --directory "$MIGRATION_BUNDLE" \
  > "$MIGRATION_CASE_DIR/logs/inspect.log" 2>&1

MIGRATION_EXIT=$?
cat "$MIGRATION_CASE_DIR/logs/inspect.log"
printf 'inspect exit=%s\n' "$MIGRATION_EXIT"
~~~

要求 exit=0。inspect 不连接数据库，只检查清单、类型、文件哈希、行数、列数和完整键唯一性。

用下面的只读脚本显示每个 CSV 的对应表和记录数：

~~~bash
python3 - "$MIGRATION_BUNDLE" <<'PY'
import json
import sys
from pathlib import Path

folder = Path(sys.argv[1])
manifest = json.loads((folder / "manifest.json").read_text(encoding="utf-8"))
for table in manifest["tables"]:
    print(table["kind"], table["name"], table["file"], table["rowCount"])
PY
~~~

和第 1 节的预期清单逐项核对。空表的 CSV 仍然存在，只含表头。

CSV 用 N 表示 NULL，V: 表示空字符串，V:后面的 Base64 表示其他值。需要查看时使用只读方式，不用 Excel 重新保存文件；修改任意 CSV 字节都会影响哈希检查。

## 10. 导入目标空间 B

此时 B 必须仍然不存在。工具会自动创建空间、Tag/Edge，然后逐条 INSERT。

在终端 A 执行：

~~~bash
java -Xmx2g -jar "$MIGRATION_JAR" import \
  --target-space manual_mig_b \
  --directory "$MIGRATION_BUNDLE" \
  --target-host 127.0.0.1 \
  --target-graph-port 9669 \
  --target-meta-port 9559 \
  --target-user root \
  --target-password-env NEBULA_PASSWORD \
  --scan-limit 1 \
  --timeout-ms 60000 \
  --schema-wait-ms 20000 \
  > "$MIGRATION_CASE_DIR/logs/import.log" 2>&1

MIGRATION_EXIT=$?
cat "$MIGRATION_CASE_DIR/logs/import.log"
printf 'import exit=%s\n' "$MIGRATION_EXIT"
~~~

要求 exit=0。创建空间和 Schema 时，工具各等待一次默认 20 秒传播时间，所以即使数据很少，也有约 40 秒的等待。

import 包含“目标空间与 CSV”的全量比较。成功报告关键字段预期为：

~~~json
{
  "matched": true,
  "sourceRechecked": false,
  "tagRows": 8,
  "edgeRows": 4
}
~~~

sourceRechecked=false 在这个步骤正常，下一步再核对源。

## 11. 同时验证源 A 和目标 B

在终端 A 执行：

~~~bash
java -Xmx2g -jar "$MIGRATION_JAR" verify \
  --target-space manual_mig_b \
  --directory "$MIGRATION_BUNDLE" \
  --source-host 127.0.0.1 \
  --source-graph-port 9669 \
  --source-meta-port 9559 \
  --source-user root \
  --source-password-env NEBULA_PASSWORD \
  --target-host 127.0.0.1 \
  --target-graph-port 9669 \
  --target-meta-port 9559 \
  --target-user root \
  --target-password-env NEBULA_PASSWORD \
  --scan-limit 2 \
  --timeout-ms 60000 \
  > "$MIGRATION_CASE_DIR/logs/verify.log" 2>&1

MIGRATION_EXIT=$?
cat "$MIGRATION_CASE_DIR/logs/verify.log"
printf 'verify exit=%s\n' "$MIGRATION_EXIT"
~~~

这里使用不同的 scan limit=2，验证相同数据在不同分页大小下仍然一致。源空间名称取自 manifest.json，不需要额外传 --source-space。

检查成功报告：

~~~bash
python3 -m json.tool "$MIGRATION_BUNDLE/verification-report.json"
~~~

验收条件必须同时满足：

- 本次 verify 进程 exit=0。
- matched=true。
- sourceRechecked=true。
- verifiedAt 对应本次校验时间。
- tagRows=8、edgeRows=4。
- rowsByTable 与第 1 节逐项相同，包括空表的 0 行。

如果本次失败，旧的成功 verification-report.json 可能仍然保留，不能仅凭文件存在或旧的 matched=true 判断成功。

## 12. 手工回读目标与主动验证失败检测

在终端 B 执行：

~~~ngql
USE manual_mig_b;
SHOW TAGS;
SHOW EDGES;
~~~

然后在当前 B 空间重新执行第 7 节全部 FETCH 语句。预期与 A 相同。每次切换空间后先看控制台提示符，避免在错误空间操作。

下面是可选的故障演练，仅针对本文新建的测试目标 B。它会故意改变一条属性，然后恢复。

在终端 B 执行：

~~~ngql
USE manual_mig_b;

UPDATE VERTEX ON all_types "v1" \
SET f64=0.2;
~~~

在终端 A 重新执行第 11 节 verify 命令。预期 exit=1，并出现 Exact property value differs。此时旧成功报告可能还在，必须以本次退出码和日志为准。

恢复原值，在终端 B 执行：

~~~ngql
USE manual_mig_b;

UPDATE VERTEX ON all_types "v1" \
SET f64=0.1;
~~~

在终端 A 再次执行第 11 节 verify，要求恢复为 exit=0、matched=true、sourceRechecked=true。

故障演练不修改源 A。不要在真实业务目标上照搬此演练。

## 13. 已有源数据时，一条命令完成搬迁

第 8—11 节是分步路线。migrate 自带 export、import、verify，不能对已经导出的 bundle 再运行 migrate，也不能导入到已存在的 manual_mig_b。

如果要额外演示一键路线，先确认 manual_mig_b_oneclick 尚不存在，使用新目标和新目录。在终端 A 执行：

~~~bash
java -Xmx2g -jar "$MIGRATION_JAR" migrate \
  --source-space manual_mig_a \
  --target-space manual_mig_b_oneclick \
  --directory "$MIGRATION_CASE_DIR/bundle_oneclick" \
  --source-password-env NEBULA_PASSWORD \
  --target-password-env NEBULA_PASSWORD \
  --scan-limit 3 \
  --timeout-ms 60000 \
  --schema-wait-ms 20000 \
  > "$MIGRATION_CASE_DIR/logs/migrate_oneclick.log" 2>&1

MIGRATION_EXIT=$?
cat "$MIGRATION_CASE_DIR/logs/migrate_oneclick.log"
printf 'migrate exit=%s\n' "$MIGRATION_EXIT"
~~~

这里省略的连接参数使用本地默认值：127.0.0.1，Graph 9669，Meta 9559，用户 root。要求最终 exit=0、matched=true、sourceRechecked=true。

如果只想完成一次搬迁，不需要运行这一节。

## 14. 复用现成的大样本与完整专项验收

### 14.1 手工搬迁现成的 127 组属性样本

当前已有字符串 VID 源空间 mig_mtzi85s0_s_a。此前验收记录为 134 条 Tag 记录、131 条 Edge 记录，共 265 条。它包含 1 MiB 级字符串、全部 256 种字节、无效 UTF-8、浮点极值/负零、整数边界和微秒等样本；先确认源没有被后续修改。

先在控制台确认新目标 manual_full_s_b 不存在。在终端 A 执行：

~~~bash
java -Xmx2g -jar "$MIGRATION_JAR" migrate \
  --source-space mig_mtzi85s0_s_a \
  --target-space manual_full_s_b \
  --directory "$MIGRATION_CASE_DIR/full_string_bundle" \
  --source-password-env NEBULA_PASSWORD \
  --target-password-env NEBULA_PASSWORD \
  --scan-limit 7 \
  > "$MIGRATION_CASE_DIR/logs/full_string_migrate.log" 2>&1

MIGRATION_EXIT=$?
cat "$MIGRATION_CASE_DIR/logs/full_string_migrate.log"
printf 'full-string migrate exit=%s\n' "$MIGRATION_EXIT"
~~~

预期 matched=true、sourceRechecked=true、tagRows=134、edgeRows=131。历史样本说明见 [真实验收记录](migration/acceptance/2026-09-13/README.md)。

整数 VID 的对应案例使用：

~~~bash
java -Xmx2g -jar "$MIGRATION_JAR" migrate \
  --source-space mig_mtzi85s0_i_a \
  --target-space manual_full_i_b \
  --directory "$MIGRATION_CASE_DIR/full_integer_bundle" \
  --source-password-env NEBULA_PASSWORD \
  --target-password-env NEBULA_PASSWORD \
  --scan-limit 11 \
  > "$MIGRATION_CASE_DIR/logs/full_integer_migrate.log" 2>&1

MIGRATION_EXIT=$?
cat "$MIGRATION_CASE_DIR/logs/full_integer_migrate.log"
printf 'full-integer migrate exit=%s\n' "$MIGRATION_EXIT"
~~~

同样先确认目标 manual_full_i_b 不存在。预期记录数仍为 134 和 131。

### 14.2 从头生成并执行完整真实验收

该命令会自动创建两组新的唯一名称 A/B、生成样本、搬迁并独立回读，不复用本文手工创建的 manual_mig_a/manual_mig_b。

在终端 A 执行：

~~~bash
cd /home/sch/nebula/nebula-java

JAVA_HOME=/usr/lib/jvm/java-8-openjdk-amd64 \
PATH=/usr/lib/jvm/java-8-openjdk-amd64/bin:$PATH \
mvn -pl migration -am test \
  -Dtest=RealClusterRoundTripTest \
  -Dsurefire.failIfNoSpecifiedTests=false \
  -DfailIfNoTests=false \
  -Dnebula.acceptance=true \
  -Dnebula.host=127.0.0.1 \
  -Dnebula.graphPort=9669 \
  -Dnebula.metaPort=9559 \
  -Dnebula.user=root \
  -Dnebula.password=nebula \
  > "$MIGRATION_CASE_DIR/logs/full_acceptance.log" 2>&1

MIGRATION_EXIT=$?
tail -n 60 "$MIGRATION_CASE_DIR/logs/full_acceptance.log"
printf 'acceptance exit=%s\n' "$MIGRATION_EXIT"
~~~

必须带 -Dnebula.acceptance=true；漏掉会跳过真实测试，Maven 仍可能显示成功。检查 Skipped: 0、日志中的 ACCEPTANCE PASSED、ACCEPTANCE_REPORT 路径，以及对应报告 status=PASSED。

测试密码由 -Dnebula.password 指定，不读取 CLI 的 NEBULA_PASSWORD。

自动验收的输出在 migration/target/acceptance/下，长期保存应复制到 target 外；mvn clean 会删除 target 中的 JAR 和这些测试文件。

## 15. 失败后如何处理

| 现象 | 操作 |
|---|---|
| Export directory must be empty | 检查是否复用了目录、把日志放进 bundle；换新空目录重新 export |
| Target space already exists | 换一个新目标名；不要再次对同一个 B 执行 import |
| Space/Tag not found | 检查 USE 和 DDL 是否成功，等待元数据传播；不要无条件重复插入 |
| Missing yield clause | FETCH 末尾补 YIELD，本文所有 FETCH 已带 YIELD |
| Parameter is not supported in vid | 手工 VID 使用字面量；工具已经处理该限制 |
| Out of range near 9223372036854775808 | INT64 属性/VID 可用 toInteger("-9223372036854775808")；最小 rank 无法经该服务端 INSERT 表达 |
| CSV SHA-256 mismatch | 文件发生变化；保留现场，重新导出，不修改哈希绕过检查 |
| INSERT failed / target is incomplete | 目标可能部分写入；修正原因后，用通过 inspect 的 bundle 导入另一个新目标 B2 |
| Exact property value differs / Missing complete key | 停止改动双方，检查源是否变化或目标是否被修改；结合当前退出码，勿使用旧报告判断 |
| Java heap space | 工具会把全量记录放入内存；按机器容量调整 -Xmx，随后按失败阶段重新操作 |
| 要求 disable_octal_escape_char=false | 字符串 VID 字面量探测未通过；检查服务端八进制转义配置，不能跳过探测继续导入 |
| Maven 缺少依赖或 JDK 错误 | 确认 JDK 8 和 Maven 可用，构建根目录及模块参数与本文一致 |

失败后不要直接执行“修正 CSV 的 rowCount/sha256”让检查通过。不要把失败目标中的部分数据误认为完整结果。本文推荐换目标名重跑，不提供自动删除已有空间的步骤。

## 16. 迁移业务数据前确认范围

- 源需要从导出到最终校验结束都停止写入和 Schema 变更；目标由本工具独占。
- 不支持 GEOGRAPHY、活跃 TTL、无 Tag 的点。
- 源的字符串 VID 和 FIXED_STRING 属性不含 NUL；普通 STRING 支持 NUL 和无效 UTF-8。
- rank=-9223372036854775808 在当前 3.6 服务端无法用 INSERT 表达，工具会提前拒绝。INT64 属性与 VID 的完整上下界已验证。
- 特殊非有限浮点值不能笼统承诺往返；工具按实际编码和重放限制拒绝。
- 索引、权限、账号、集群配置不在本工具搬迁范围内。
- 全量 scan 比较复用了导出扫描通道，没有建立跨空间快照；生产校验与第 14.2 节的独立预知样本 FETCH 验收不同。
- CLI 每侧的 --host 同时用于 Graph 和 Meta，端口可分别设置；当前手工案例的同机部署满足这一条件。
- 最终成功凭据为本次退出码 0，加上本次 verifiedAt、matched=true、sourceRechecked=true 和符合预期的逐表记录数。
