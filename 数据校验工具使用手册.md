# NebulaGraph 数据搬迁后校验工具使用手册

适用工具：`nebula-data-verifier-3.8.4.jar`
文档日期：2026-09-13
当前工程：`/home/sch/nebula/nebula-java`

工具按固定的点、边 ID 清单，分别通过 nebula-java 的 `FETCH` 查询源、目标数据，将原生查询结果保存为文件，再离线比较。源、目标可以在不同网络，采集时不需要同时连接两个集群。

日常校验命令只读数据库；数据导出、创建目标空间和 INSERT 写入由搬迁工具完成。

## 1. 先了解校验结论的范围

| 对象 | 参与比较的内容 |
|---|---|
| 点 | VID、实际全部 Tag、各 Tag 的完整属性名及值，包括无属性 Tag 的存在性 |
| 边 | SRC、Edge 名称、RANK、DST，以及完整属性名及值 |
| 相关 Schema | 属性名称、类型、可空性 |

字符串按原始字节比较，浮点数按原始位模式比较，时间保留微秒，NULL 与空字符串区分。不同空间的内部 Tag/Edge 数字 ID 不参与比较。

**MATCH 表示清单内所有对象都成功读取，且上述内容一致。** 它不证明清单覆盖全库，不检查全库点边数量，也不查找清单外目标多出的点或边。清单内某个点多出 Tag 会被发现。未被任何清单对象引用的空 Tag/Edge Schema、默认表达式、注释、索引、权限及 TTL 配置不属于本工具的比较范围。

## 2. 获取 JAR 和检查环境

当前已构建的独立可执行文件：

```text
/home/sch/nebula/nebula-java/verification/target/nebula-data-verifier-3.8.4.jar
```

部署到其他机器时，复制这一个 JAR 即可运行校验，不需要另放 client JAR、搬迁 JAR 或 nebula-console。需要运行搬迁操作的机器仍需另外准备搬迁工具。

本工程使用 JDK 8 构建；真实集群测试使用 NebulaGraph 3.6，最终 JAR 在本机 Java 17 上也已运行通过。其他服务端版本需在对应环境验收。

```bash
java -version
java -jar /home/sch/nebula/nebula-java/verification/target/nebula-data-verifier-3.8.4.jar --help
```

只有 `capture` 需要访问数据库，连接的是 graphd，默认 `127.0.0.1:9669`。账号需要能够读取相应空间、Tag/Edge Schema 及执行 FETCH。校验工具不需要连接 Meta、Storage 端口。

工具逐条查询，并在内存中处理记录。示例使用 `-Xmx2g`，这只是堆内存配置示例，不代表任意规模数据都能在 2 GiB 中处理；需按实际文件大小调整。

## 3. 四个命令分别做什么

| 命令 | 输入 | 输出 | 是否连接数据库 |
|---|---|---|---|
| `prepare` | 本工程搬迁工具的完整导出目录 | 固定 ID 计划目录 | 否 |
| `prepare-ids` | 已有点 ID、边 ID CSV 和 VID 类型 | 固定 ID 计划目录 | 否 |
| `capture` | 计划目录、某一侧空间及连接参数 | 该侧 FETCH 采集目录 | 是，只连这一侧 |
| `compare` | 源、目标采集目录 | 比较报告目录 | 否 |

`prepare` 和 `prepare-ids` 是生成计划的两种入口，选一种即可。**一个校验批次只生成一次计划，然后将整个计划目录复制到目标环境。** 即使输入 ID 完全一样，重新生成也会产生不同的 `planId`，两次计划的采集结果不能混用。

所有参数采用 `--参数名 参数值`，不支持 `--参数名=参数值`、短参数或重复参数。目录名包含空格时需要加引号。

所有 `--out` 都必须不存在或为空。工具会创建必要的父目录；不要把日志预先写入 `--out` 指定的目录，否则会因目录非空而拒绝执行。

## 4. 推荐的异地搬迁操作顺序

完整顺序如下：

```text
源端停止写入、Schema 变更，并确保数据不会因 TTL 过期
  → 搬迁工具 scan 导出数据
  → prepare 生成固定 ID 计划
  → capture 保存源端 FETCH 基准，确认 COMPLETE
  → 源端可以恢复工作
  → 将导出文件和计划传到目标端，执行目标导入
  → 等目标导入完成且数据稳定，capture 保存目标 FETCH
  → 将源、目标采集目录放到同一台机器，compare 离线比较
```

源数据必须从导出前保持稳定，直到**数据导出和源端 FETCH 基准均完成**。如果两者之间源发生了变化，导出数据和校验基准就可能不是同一份数据。目标采集期间同样需要停止相关写入、Schema 变更，并确保数据不会自动过期。

逐条 FETCH 不是数据库一致性快照。工具不会自动停止业务写入、禁用 TTL 或修改数据库配置；这些状态需要由操作人员事先安排。源恢复写入后，报告比较的是已保存的历史基准，不是比较时源集群的实时状态。

### 4.1 源端：导出搬迁数据

如果已经有有效导出目录，而且源从导出时起一直保持稳定，可直接进入下一步。

下面是与现有搬迁工具衔接的示例。将空间 `A`、地址 `192.0.2.10`、端口和密码换为实际配置；`/data/nebula-check/run-001` 为本次批次目录，可以按环境替换。

```bash
export NEBULA_SOURCE_PASSWORD='替换为源集群密码'

java -Xmx2g -jar /home/sch/nebula/nebula-java/migration/target/migration-3.8.4.jar export \
  --source-space A \
  --source-host 192.0.2.10 \
  --source-graph-port 9669 \
  --source-meta-port 9559 \
  --source-user root \
  --source-password-env NEBULA_SOURCE_PASSWORD \
  --directory /data/nebula-check/run-001/export-A
```

本条命令属于搬迁工具，会连接 Graph/Meta/Storage；其网络要求与校验工具不同。更多搬迁参数见[搬迁工具说明](migration/README.md)。必须确认导出成功后再继续。

### 4.2 源端：生成固定 ID 计划

```bash
java -Xmx2g -jar /home/sch/nebula/nebula-java/verification/target/nebula-data-verifier-3.8.4.jar prepare \
  --migration-dir /data/nebula-check/run-001/export-A \
  --out /data/nebula-check/run-001/plan
```

`--migration-dir` 必须包含搬迁工具生成的 `manifest.json` 以及其中列出的全部 CSV。不能只给单个 CSV，也不能把 nebula-console 的普通 CSV 直接放进这个入口。

命令会检查导出格式、CSV SHA-256、表头、记录数及标识唯一性，然后从 Tag/Edge 数据行提取 ID。同一 VID 跨多个 Tag 去重；边的 SRC、DST 也会加入点清单。因此计划点数不同于搬迁工具所有 Tag 的行数之和。

成功后生成：

```text
plan/
  plan.json
  vertices.csv
  edges.csv
```

生成后保持这三个文件不变。CSV 的传输或换行转换造成内容变化时，后续校验和检查会拒绝文件。

### 4.3 源端：保存 FETCH 基准

```bash
java -Xmx2g -jar /home/sch/nebula/nebula-java/verification/target/nebula-data-verifier-3.8.4.jar capture \
  --plan /data/nebula-check/run-001/plan \
  --space A \
  --host 192.0.2.10 \
  --port 9669 \
  --user root \
  --password-env NEBULA_SOURCE_PASSWORD \
  --timeout-ms 60000 \
  --out /data/nebula-check/run-001/source
```

命令紧接着检查退出码：

```bash
echo $?
```

退出码必须为 `0`。同时确认 `source/manifest.json` 中 `status` 为 `COMPLETE`，`missingCount`、`errorCount` 都为 `0`，`errors` 为空数组。连接或空间检查阶段失败时，可能根本没有 manifest；这也不能作为有效基准。

源导出和源基准均成功后，源端才能恢复写入。若本步骤失败，先处理原因并保持源数据稳定，再使用新的输出目录重新采集；若源已经发生变化，应重新建立相互一致的导出文件和源基准。

### 4.4 传递文件，完成目标导入

按现场允许的文件传递方式复制整个目录，保留文件原始内容：

| 文件或目录 | 放到哪里 | 用途 |
|---|---|---|
| 独立校验 JAR | 源采集机、目标采集机、比较机 | 各机执行相应命令 |
| `export-A/` | 目标搬迁环境 | 由搬迁工具导入 |
| `plan/` | 目标采集机；建议比较机也留存 | 查询完全相同的点、边 |
| `source/` | 最终比较机 | 源端历史基准 |
| 后续生成的 `target/` | 最终比较机 | 目标端采集结果 |

目标采集机只需计划目录和目标连接信息；它不必直接访问源集群。最终比较机只需两份采集目录和 JAR，不需要访问任何集群。

目标端使用原搬迁流程导入。下面给出本工程搬迁工具示例，假设导出目录已复制到相同路径，目标地址为 `192.0.2.20`，目标新空间为 `B`：

```bash
export NEBULA_TARGET_PASSWORD='替换为目标集群密码'

java -Xmx2g -jar /home/sch/nebula/nebula-java/migration/target/migration-3.8.4.jar import \
  --target-space B \
  --target-host 192.0.2.20 \
  --target-graph-port 9669 \
  --target-meta-port 9559 \
  --target-user root \
  --target-password-env NEBULA_TARGET_PASSWORD \
  --directory /data/nebula-check/run-001/export-A
```

该搬迁工具要求目标空间不存在。必须等导入命令成功退出、数据稳定后再进行目标采集。部署路径不同的机器需要将 `-jar` 后的路径换成本机实际路径。

### 4.5 目标端：保存目标 FETCH 结果

使用源端复制来的原计划：

```bash
java -Xmx2g -jar /home/sch/nebula/nebula-java/verification/target/nebula-data-verifier-3.8.4.jar capture \
  --plan /data/nebula-check/run-001/plan \
  --space B \
  --host 192.0.2.20 \
  --port 9669 \
  --user root \
  --password-env NEBULA_TARGET_PASSWORD \
  --timeout-ms 60000 \
  --out /data/nebula-check/run-001/target
```

成功要求与源采集相同：退出 `0`、manifest 为 `COMPLETE`。如果输出 `INCOMPLETE`，可保留文件用于诊断和离线错误报告，但它不能通过一致性校验。

### 4.6 比较机：完全离线比较

假设源、目标采集目录已经放在比较机的以下路径：

```bash
java -Xmx2g -jar /home/sch/nebula/nebula-java/verification/target/nebula-data-verifier-3.8.4.jar compare \
  --source /data/nebula-check/run-001/source \
  --target /data/nebula-check/run-001/target \
  --out /data/nebula-check/run-001/result
```

本步骤不需要密码和集群地址。报告文件为：

```text
/data/nebula-check/run-001/result/comparison-report.json
```

不要直接用 `diff` 比较整个源、目标目录：manifest 包含空间名、采集时间等本来就不同的元数据。应使用 `compare` 解析结构，按对象键、字段和相关 Schema 比较。

## 5. 当前本机快速体验

本节使用开发验收中保留的已搬迁空间，操作只读数据库，不创建空间、不重新搬迁。它用于熟悉四步校验命令，不代表重新执行“源基准 → 搬迁 → 目标基准”的完整时序。如果样本空间或导出目录已被清理，需要换成实际存在的数据。

在同一个 Bash 终端按顺序执行，每步成功后再执行下一步。重复体验时，将 `manual-run-001` 换成新的批次目录：

```bash
VERIFIER_JAR='/home/sch/nebula/nebula-java/verification/target/nebula-data-verifier-3.8.4.jar'
CHECK_ROOT='/home/sch/nebula/nebula-java/verification/target/manual-run-001'
export NEBULA_PASSWORD='nebula'

java -Xmx2g -jar "$VERIFIER_JAR" prepare \
  --migration-dir /home/sch/nebula/nebula-java/migration/target/acceptance/mtzi85s0/bundle_s \
  --out "$CHECK_ROOT/plan"

java -Xmx2g -jar "$VERIFIER_JAR" capture \
  --plan "$CHECK_ROOT/plan" --space mig_mtzi85s0_s_a \
  --host 127.0.0.1 --port 9669 --user root \
  --out "$CHECK_ROOT/source"

java -Xmx2g -jar "$VERIFIER_JAR" capture \
  --plan "$CHECK_ROOT/plan" --space verify_20260913_s_b \
  --host 127.0.0.1 --port 9669 --user root \
  --out "$CHECK_ROOT/target"

java -Xmx2g -jar "$VERIFIER_JAR" compare \
  --source "$CHECK_ROOT/source" --target "$CHECK_ROOT/target" \
  --out "$CHECK_ROOT/result"
```

在样本未改变时，预期为 127 个点、131 条边，`comparedRecords=258`，结果 `MATCH`。这些数字是样本清单对象数，不是工具对全库数量作出的结论。

## 6. 已有 ID 清单时如何使用

没有本工程搬迁导出文件时，使用 `prepare-ids`。它要求同时提供点、边两个 CSV；只校验点时边文件保留表头，只校验边时点文件保留表头。整个计划至少包含一个对象。

### 6.1 CSV 格式

点文件 `vertices.csv`，表头必须是 `vid`：

```csv
vid
V:dXNlcjE=
V:dXNlcjI=
```

边文件 `edges.csv`，表头及列顺序必须是 `src,edge,rank,dst`：

```csv
src,edge,rank,dst
V:dXNlcjE=,V:Zm9sbG93,V:Nw==,V:dXNlcjI=
```

这表示两个字符串 VID `user1`、`user2`，以及 `user1 -[follow@7]-> user2`。

| 单元格 | 编码规则 |
|---|---|
| 字符串 VID | `V:` + 原始字节的标准 Base64 |
| INT64 VID | `V:` + 规范十进制 ASCII 的标准 Base64 |
| `edge` | `V:` + Edge 名称 UTF-8 字节的标准 Base64，不能填写内部数字 EdgeType |
| `rank` | `V:` + 规范十进制 ASCII 的标准 Base64 |

整数例：`1` 编为 `V:MQ==`，`-7` 编为 `V:LTc=`。字符串 VID `"1"` 与整数 VID `1` 的载荷都可为 `V:MQ==`，含义由计划的 VID 类型决定。NULL ID 不允许；空字符串 VID 的单元格是 `V:`。

Base64 必须带必要的 `=` 填充、不能换行。CSV 不使用额外引号包装单元格，禁止 BOM、空白行、重复点 VID 或重复完整边键。不要通过字符解码/重新编码处理含无效 UTF-8 的原始字符串 VID。

### 6.2 生成计划并采集

假设上述文件保存为 `/data/vertices.csv` 和 `/data/edges.csv`，源、目标空间 VID 类型均为 `FIXED_STRING(128)`：

```bash
java -Xmx2g -jar /home/sch/nebula/nebula-java/verification/target/nebula-data-verifier-3.8.4.jar prepare-ids \
  --vertices /data/vertices.csv \
  --edges /data/edges.csv \
  --vid-type 'FIXED_STRING(128)' \
  --out /data/nebula-check/run-ids-001/plan
```

整数 VID 空间使用 `--vid-type INT64`。两端实际空间的 VID 类型必须与计划匹配；例如计划为 `FIXED_STRING(128)`、目标为 `FIXED_STRING(256)`，本版本会拒绝采集。

后续仍然按第 4 节执行源采集、目标采集、离线比较。`prepare-ids` 不会自动把边端点加入点清单；如果还要校验端点的 Tag 和属性，需要显式把端点 VID 写入点文件。

## 7. 参数完整说明

| 命令 | 必填参数 | 含义 |
|---|---|---|
| `prepare` | `--migration-dir`、`--out` | 搬迁导出目录、计划输出目录 |
| `prepare-ids` | `--vertices`、`--edges`、`--vid-type`、`--out` | 点 ID CSV、边 ID CSV、VID 类型、计划输出目录 |
| `capture` | `--plan`、`--space`、`--out` | 固定计划目录、该侧实际空间名、采集输出目录 |
| `compare` | `--source`、`--target`、`--out` | 源采集目录、目标采集目录、报告输出目录 |

`capture` 还支持以下可选参数：

| 参数 | 默认值 | 说明 |
|---|---|---|
| `--host` | `127.0.0.1` | 这一侧 graphd 主机地址 |
| `--port` | `9669` | graphd 端口 |
| `--user` | `root` | 登录用户 |
| `--password-env` | `NEBULA_PASSWORD` | 保存密码的环境变量名，不是密码本身 |
| `--timeout-ms` | `60000` | Graph 请求超时，毫秒，必须大于 0 |

例如 `--password-env NEBULA_TARGET_PASSWORD` 会读取 `NEBULA_TARGET_PASSWORD` 的值。校验 CLI 不会自动选择搬迁工具的 `NEBULA_SOURCE_PASSWORD`/`NEBULA_TARGET_PASSWORD`，必须显式指定，或改用默认的 `NEBULA_PASSWORD`。没有 `--password`、`--source-host`、`--target-host`、`--meta-port`、`--scan-limit` 等校验参数。

密码只用于连接，不写入计划或采集文件。示例中的 `export` 适合演示；正式操作可通过环境允许的凭据注入方式设置变量。

## 8. 如何判断成功、差异和错误

### 8.1 命令退出码

| 命令/状态 | 退出码 | 处理方式 |
|---|---:|---|
| `prepare` / `prepare-ids` 成功 | 0 | 保留计划，继续采集 |
| `capture` 为 `COMPLETE` | 0 | 可作为正式比较输入 |
| `capture` 失败或 `INCOMPLETE` | 1 | 查明缺失/错误原因，不能宣称一致 |
| `compare` 为 `MATCH` | 0 | 清单对象内容一致 |
| `compare` 为 `DIFFERENT` | 2 | 数据或相关 Schema 有差异 |
| `compare` 为 `ERROR` 或命令失败 | 1 | 输入/查询/完整性异常，不能完成有效一致性判定 |

自动化脚本要区分退出码 1 和 2。终端使用 `echo $?` 时，必须紧跟要检查的命令；其他命令会覆盖上一条退出码。参数错误或输出目录不可用时，不保证能生成报告文件。

### 8.2 采集目录内的对象状态

| `records.jsonl` 的 `status` | 含义 |
|---|---|
| `OK` | 成功读取并核对原生类型、标识、属性结构 |
| `MISSING` | 指定边无返回，或指定点无返回/没有实际 Tag 成员 |
| `ERROR` | 查询失败、返回结构异常、不支持的类型、Schema 不匹配等 |

一个多 Tag 点少了其中一个无属性 Tag，通常仍能完整采集，比较时结果为 `DIFFERENT`。如果整个点已没有任何 Tag，或一条清单边不存在，则采集为 `INCOMPLETE`，比较结果为 `ERROR`。即使两侧都缺失同一个对象，或者出现完全相同的查询错误，也不会得到 `MATCH`。

### 8.3 比较报告的主要字段

| 字段 | 含义 |
|---|---|
| `scope` | 固定为 `CONTENTS_OF_LISTED_OBJECTS_ONLY` |
| `status` / `matched` | 最终状态；只有 MATCH 对应 true |
| `planId` | 使用的固定计划身份 |
| `sourceSpace` / `targetSpace` | 两侧实际空间名 |
| `sourceCapturedAt` / `targetCapturedAt` | 两侧开始采集的时间；带 `Z` 表示 UTC |
| `comparedRecords` | 两侧均为 OK、实际参与字段比较的对象数 |
| `differentRecords` | 已比较对象中存在字段差异的对象数 |
| `errorRecords` | 至少一侧不是 OK 的对象数，包括 MISSING |
| `differenceFields` | 记录的差异项总数，也包含 Schema/采集错误项 |
| `differences` | 展示的差异详情 |
| `detailsTruncated` | 差异详情是否达到展示上限 |

仅 Schema 不同时，`differentRecords` 可以为 `0`，但整体仍是 `DIFFERENT`；不能只看这一计数判断成功。最终以退出码及 `status`/`matched` 为准。

真实浮点差异报告的一条明细如下：

```json
{
  "field": "tag/YWxsX3R5cGVz/prop/ZjY0",
  "key": "vertex|V:aWQnIlwNCvCfmIBlzIE=",
  "reason": "FIELD_DIFFERS",
  "sourceValue": "[\"float\",\"3fb999999999999a\"]",
  "targetValue": "[\"float\",\"3fb999999999999b\"]"
}
```

`YWxsX3R5cGVz` 和 `ZjY0` 解码后分别是 Tag `all_types` 和属性 `f64`。源为 DOUBLE `0.1`，目标为它的下一个较大可表示值，原始位相差 1，因此判定不同。

报告最多展示 1,000 条差异，每个较长值只展示前 256 个字符。完整字段值保留在源、目标各自的 `records.jsonl` 中；展示截断不影响差异判定。

## 9. 输出文件如何保存和查看

```text
run-001/
  export-A/                     搬迁数据文件
  plan/
    plan.json                   计划 ID、VID 类型、ID 文件摘要和数量
    vertices.csv                点 ID 清单
    edges.csv                   边完整 ID 清单
  source/
    manifest.json               源采集状态、相关 Schema、记录摘要
    records.jsonl               每个清单对象的源 FETCH 原生编码
  target/
    manifest.json               目标采集状态、相关 Schema、记录摘要
    records.jsonl               每个清单对象的目标 FETCH 原生编码
  result/
    comparison-report.json      离线比较报告
```

长期保存、异地传输或交付复核时，保留完整 `plan/`、`source/`、`target/` 和 `result/`。单独保存 manifest 或比较报告，不能重新执行内容比较。重新比较可以使用已有两份完整采集目录，但必须为 `--out` 指定新的报告目录。

采集结果是 JSON Lines，每行一个对象。属性字节编码为 Base64，浮点编码为原始位，时间编码为原生字段；文件不是供 Excel 展示和编辑的普通数据表。不要手工改内容、改换行或重新保存后继续当作原基准使用。

例如字符串 `abc<NUL>def` 保存为原生载荷 `["string","YWJjAGRlZg=="]`，空字符串为 `["string",""]`，NULL 为 `["null"]`。这些内容作为外层 JSON 的字符串保存，外层会再转义双引号。

日志可以重定向到结果目录外，例如：

```bash
java -Xmx2g -jar /home/sch/nebula/nebula-java/verification/target/nebula-data-verifier-3.8.4.jar compare \
  --source /data/nebula-check/run-001/source \
  --target /data/nebula-check/run-001/target \
  --out /data/nebula-check/run-001/result-second \
  > /data/nebula-check/run-001/compare-second.log 2>&1
```

## 10. 常见问题处理

| 现象或错误片段 | 原因与处理 |
|---|---|
| `Missing password environment variable` | 检查变量已 export，且 `--password-env` 指定的是变量名；变量须在启动 Java 的环境中存在 |
| 无法连接、登录失败、找不到空间 | 核对该侧 Graph 地址、端口、用户、密码、空间名；这种失败可能没有输出 manifest |
| `Output directory must be empty` | 指定了旧输出目录、失败遗留目录，或把日志放进输出目录；保留旧文件，改用新目录 |
| `different plans` | 两侧分别生成过计划或混用了不同批次；使用同一份原计划重新采集所需的一侧 |
| `SHA-256 mismatch` | 文件缺失、损坏、被编辑或传输时发生变化；从原始完整目录重新复制并重试，不能通过手改摘要绕过检查 |
| `Space VID type differs from plan` | 清单 VID 类型与实际空间不一致；核对是否选错空间、计划或空间定义 |
| `Non-canonical graph-key Base64` / ID 格式错误 | 检查是否缺 `=`、有引号、额外空格、BOM、空白行或原始 VID 未编码 |
| `INCOMPLETE`、`MISSING` | 先查对应记录的 key/error，确认对象真实存在、导入已结束、点实际带有 Tag、数据未因 TTL 消失 |
| `Schema changed during capture` | 相关 Schema 在采集期间变化；恢复稳定后用新目录重新采集 |
| `disable_octal_escape_char=false` 相关错误 | 字符串 VID 的字节转义探测未通过；核对服务端设置。工具不会自动改配置 |
| 内存不足、请求超时 | 按机器容量增加 `-Xmx`，检查服务端及单条数据限制，必要时调整 `--timeout-ms`；失败输出不能当作完整结果 |
| `DIFFERENT` | 查看差异 key、field 和两侧完整编码；修复目标后，保持目标稳定，用原计划及新的目标采集/报告目录重新校验 |

工具没有并发优化、自动重试、断点续传或自动修复。对失败目录原地重跑会被拒绝。如果源历史基准有效，可以保留它，只重新采集修正后的目标；如果源基准本身无效，就需要重新建立有效基准。

## 11. 数据支持与当前边界

支持 BOOL、INT8/16/32/64、FLOAT、DOUBLE、STRING、FIXED_STRING(N)、DATE、TIME、DATETIME、TIMESTAMP、DURATION，以及正常 NULL。实际清单对象中出现 GEOGRAPHY、异常 NULL 或不支持的返回类型时拒绝成功。

沿用本项目已明确的源数据前提：点带有 Tag，字符串 VID 与 FIXED_STRING 属性不含 NUL。普通 STRING 可以含 NUL、引号、反斜杠、CR/LF、emoji、组合字符、无效 UTF-8 及任意字节。长字符串受内存及服务器/RPC 限制，不承诺无限长度。比较对象是服务端接口实际返回的数据，不能恢复写入或读取接口之前已经丢失的信息。

本机 NebulaGraph 3.6 的 FETCH rank 语法不能表达 `-9223372036854775808`，工具明确拒绝这个 rank；INT64 VID 和 INT64 属性最小值仍受支持。字符串 VID 使用每字节三位八进制转义，开始采集时会执行只读探测。

## 12. 已完成的真实验收及其边界

已在用户提供的本机 NebulaGraph 3.6 中，通过搬迁工具将真实 CSV 导入新的目标空间，再使用校验工具分别 FETCH 源、目标并离线比较。字符串 VID、整数 VID 两组各含 127 个点与 131 条边，共 516 个清单对象，正常结果均为 MATCH。

真实修改目标的浮点单个位、删除/增加无属性 Tag、删除无属性边均被发现；恢复数据后重新匹配。最终独立 JAR 的 `prepare`、两侧 `capture`、`compare` 也已通过命令行验证。另有 78 项校验工具单元测试、18 项 Client 回归测试通过。

**已完成验收的源 FETCH 基准是在目标导入完成后采集的，期间源保持不变。尚未严格按第 4 节“先源基准、再导入、再目标基准”的时序完整重跑，也未在两台网络隔离机器上验收。** 第 4 节描述的是建议的实际操作顺序，应与已经执行过的验收范围区分。

详细证据见[真实集群验收记录](verification/acceptance/2026-09-13/README.md)。本手册的日常命令不会修改数据库；开发用真实负向测试会修改并恢复其指定的测试目标，不能把业务空间直接当作该测试目标。

## 13. 从源码重新构建

在当前工程使用 JDK 8 构建：

```bash
cd /home/sch/nebula/nebula-java
JAVA_HOME=/usr/lib/jvm/java-8-openjdk-amd64 PATH=/usr/lib/jvm/java-8-openjdk-amd64/bin:$PATH \
  mvn -pl verification -am package -DskipTests -Dmaven.javadoc.skip=true
```

该命令会构建独立 JAR，但跳过测试。生成文件仍为：

```text
/home/sch/nebula/nebula-java/verification/target/nebula-data-verifier-3.8.4.jar
```

实现和开发测试入口见[模块说明](verification/README.md)及[命令行入口代码](verification/src/main/java/com/vesoft/nebula/verification/VerifierCli.java)。
